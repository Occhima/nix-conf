;; keybindgs remaps


;; evil config


;; (map! :localleader
;; :n "c" #'+chatgpt/open
;; :n  "g" #'+consult-gh/open
;; :n  "n" #'+nyxt/connect
;; :n  "f" #'affe-find
;; :n  "k" #'affe-grep
;; :n  "w" #'biome
;; :n  "m" #'osm-home
;; )

;; remapping normalmode to esc, also

;; (map!
;;  :leader
;;  (:prefix ("m" . "+open")
;;   :desc "Open EAT" "t" #'eat
;;   )
;;  )
