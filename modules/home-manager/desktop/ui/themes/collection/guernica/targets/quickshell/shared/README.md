# Shared Guernica QML primitives

These compositor-neutral services and widgets form Guernica's small QML
toolkit. The `guernica-base` and `guernica-ukishima` targets each assemble an
independent Quickshell config from this toolkit plus their own variant source.
Neither variant imports or copies the other.
