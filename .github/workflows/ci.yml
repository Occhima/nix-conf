# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/ci.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  build:
    name: Representative builds
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: DeterminateSystems/nix-installer-action@33c9ab3ef95cd57c164d9d6eb1f9a46338538d41
      - uses: DeterminateSystems/flakehub-cache-action@77c6bddd7d747943530aaa578c57f233ee5d920e
      - name: Graphical NixOS toplevel
        run:
          nix build .#nixosConfigurations.aerodynamic.config.system.build.toplevel
          --no-link
      - name: Standalone Home Manager activation package
        run: nix build .#homeConfigurations.occhima.activationPackage --no-link
      - name: run-vm package
        run: nix build .#run-vm --no-link
    timeout-minutes: 120
  eval-drvpaths:
    name: Host and home drvPath evaluation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: DeterminateSystems/nix-installer-action@33c9ab3ef95cd57c164d9d6eb1f9a46338538d41
      - uses: DeterminateSystems/flakehub-cache-action@77c6bddd7d747943530aaa578c57f233ee5d920e
      - name: Evaluate all drvPaths
        run: |-
          set -euo pipefail
          for host in aerodynamic beyond crescendoll steammachine; do
            nix eval ".#nixosConfigurations.$host.config.system.build.toplevel.drvPath"
          done
          nix eval .#nixosConfigurations.voyager.config.system.build.isoImage.drvPath
          nix eval .#homeConfigurations.occhima.activationPackage.drvPath
    timeout-minutes: 120
  flake-check:
    name: Flake Check (format, lint, tests, freshness)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: DeterminateSystems/nix-installer-action@33c9ab3ef95cd57c164d9d6eb1f9a46338538d41
      - uses: DeterminateSystems/flakehub-cache-action@77c6bddd7d747943530aaa578c57f233ee5d920e
      - uses: DeterminateSystems/flake-checker-action@de924abd783455e8429c858962b9e43062d19da1
      - run: nix flake check --show-trace
    timeout-minutes: 120
  no-ifd:
    name: Flake Check without IFD
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: DeterminateSystems/nix-installer-action@33c9ab3ef95cd57c164d9d6eb1f9a46338538d41
      - uses: DeterminateSystems/flakehub-cache-action@77c6bddd7d747943530aaa578c57f233ee5d920e
      - run: nix flake check --show-trace --option allow-import-from-derivation false
    timeout-minutes: 120
  scanning:
    name: GitGuardian scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - env:
          GITGUARDIAN_API_KEY: ${{ secrets.GITGUARDIAN_API_KEY }}
          GITHUB_DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          GITHUB_PULL_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          GITHUB_PUSH_BASE_SHA: ${{ github.event.base }}
          GITHUB_PUSH_BEFORE_SHA: ${{ github.event.before }}
        name: GitGuardian scan
        uses: GitGuardian/ggshield/actions/secret@e2375101498ed56425e6a44f255f9f300ecfc2c3
    timeout-minutes: 120
name: Flake Check
"on":
  pull_request: {}
  push:
    branches: "[main]"
